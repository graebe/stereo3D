{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Tests",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/runTests.bin",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "Compile Project", // This will compile your project before running the tests
            "osx": {
                "MIMode": "lldb"
            }
        }
    ]
}